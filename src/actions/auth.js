export const startLogin = () => {
    return dispatch => {
        return; // Make authentication call to the server
    };
};

export const startLogout = () => {
    return dispatch => {
        return // Make authentication call to server
    }
}